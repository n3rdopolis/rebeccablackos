diff --git a/kioclient/kioclient.cpp b/kioclient/kioclient.cpp
index ee89561..7a15b1a 100644
--- a/kioclient/kioclient.cpp
+++ b/kioclient/kioclient.cpp
@@ -32,6 +32,9 @@
 #include <KAboutData>
 #include <KLocalizedString>
 
+#include <QtCore/qurl.h>
+#include "kopenwithdialog.h"
+#include <QWidget>
 #include <QDBusConnection>
 #include <QCommandLineParser>
 #include <QFileDialog>
@@ -324,6 +327,15 @@ bool ClientApp::doIt(const QCommandLineParser& parser)
                              argc == 3 ? parser.positionalArguments().last() : QString(),
                              true );
     }
+    else if ( command == QLatin1String("appmenu") )
+    {
+      QList<QUrl> list;
+      KOpenWithDialog dlg(list, i18n("Select an Application to run..."), QString(), 0);
+      dlg.hideRunInTerminal();
+      if (dlg.exec()) {
+          KRun::run(*dlg.service(), list, 0);
+      }
+    }
 #endif
     else if ( command == QLatin1String("download") )
     {
